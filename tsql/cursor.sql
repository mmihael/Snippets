DECLARE @Id as INT;

DECLARE @C as CURSOR;

SET @C = CURSOR FOR SELECT Id FROM schema.User;

OPEN @C;

FETCH NEXT FROM @C INTO @Id;

WHILE @@FETCH_STATUS = 0
BEGIN
PRINT @Id;
FETCH NEXT FROM @C INTO @Id;
END

CLOSE @C;
DEALLOCATE @C;
